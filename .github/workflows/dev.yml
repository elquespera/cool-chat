name: CoolChat Dev

on:
  push:
    branches: ["develop"]

jobs:
  build:
    runs-on: ["self-hosted", "dev"]

    strategy:
      matrix:
        node-version: [20.x]

    steps:
      - uses: actions/checkout@v4

      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}

      - name: Install dependencies
        run: bun i

      - name: Create .env file
        run: echo "${{secrets.ENV_FILE_DEV }}" > .env

      # - name: Next.Js cache
      #   uses: jongwooo/next-cache@v1

      - name: Build project
        run: npm run build --if-present

      - name: SERVER - Install dependencies
        run: cd server && bun i

      - name: SERVER - Create.env file
        run: echo "${{secrets.ENV_FILE_SERVER_DEV }}" > ./server/.env

      - name: Restart SERVER process with pm2
        run: pm2 reload cool-chat-dev-server

      - name: Restart process with pm2
        run: pm2 reload cool-chat-dev
