{
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "dev": "bun run --env-file=../.env --hot ./index.ts",
    "start": "bun run --env-file=../.env ./index.ts"
  },
  "dependencies": {
    "drizzle-orm": "^0.31.4"
  }
}
